<UserControl x:Class="HoangTranManhDungWPF.Views.Admin.RoomManagementView"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Search by Room Number:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Search" Width="80" Margin="10,0,0,0"
                    Command="{Binding SearchCommand}"/>
            <Button Content="Clear" Width="80" Margin="10,0,0,0"
                    Command="{Binding LoadRoomsCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rooms}"
                  SelectedItem="{Binding SelectedRoom}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="50"/>
                <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="100"/>
                <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType.RoomTypeName}" Width="120"/>
                <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="*"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="70"/>
                <DataGridTextColumn Header="Price/Date" Binding="{Binding RoomPricePerDate, StringFormat='C'}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add New" Width="100" Margin="5"
                    Command="{Binding AddRoomCommand}"/>
            <Button Content="Update" Width="100" Margin="5"
                    Command="{Binding UpdateRoomCommand}"/>
            <Button Content="Delete" Width="100" Margin="5"
                    Command="{Binding DeleteRoomCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>