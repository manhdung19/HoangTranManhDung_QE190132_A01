<UserControl x:Class="HoangTranManhDungWPF.Views.Customer.ProfileView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="600">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Customer ID:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Padding="3"
                 Text="{Binding CurrentCustomer.CustomerID}" IsReadOnly="True"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" VerticalAlignment="Center" Margin="5" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Padding="3"
                 Text="{Binding CurrentCustomer.EmailAddress}" IsReadOnly="True"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Full Name:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Padding="3"
                 Text="{Binding CurrentCustomer.CustomerFullName, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Telephone:" VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Padding="3"
                 Text="{Binding CurrentCustomer.Telephone, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Birthday:" VerticalAlignment="Center" Margin="5"/>
        <DatePicker Grid.Row="4" Grid.Column="1" Margin="5" Padding="3"
                    SelectedDate="{Binding CurrentCustomer.CustomerBirthday, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="5" Grid.Column="1" Content="Update Profile" Width="120"
                HorizontalAlignment="Left" Margin="5,20,0,0"
                Command="{Binding UpdateProfileCommand}"/>
    </Grid>
</UserControl>
